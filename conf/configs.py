#!/usr/bin/python

# Credentials for Mysql instance
# replace with actual credentials
mysql_creds = {
    "aws1" : {
            "user":"user",
            "password":"password",
            "host":"x.x.x.x"
    }
}